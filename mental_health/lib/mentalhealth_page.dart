import 'package:flutter/material.dart';

// Define the MentalHealthPage widget
class MentalHealthPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mental Health Page Content'),
    );
  }
}
