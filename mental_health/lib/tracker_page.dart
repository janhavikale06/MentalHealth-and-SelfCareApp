import 'package:flutter/material.dart';

// Define the TrackersPage widget
class TrackersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Trackers Page Content'),
    );
  }
}